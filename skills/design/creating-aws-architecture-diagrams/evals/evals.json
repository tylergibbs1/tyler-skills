{
  "skill_name": "creating-aws-architecture-diagrams",
  "evals": [
    {
      "skills": ["creating-aws-architecture-diagrams"],
      "query": "Create a 4K AWS PNG showing our current monthly batch runtime: one EventBridge schedule starts one private Fargate task, the task tracks progress in DynamoDB, writes ordinary output to one S3 bucket and retained evidence to an Object Lock bucket, and sends logs to CloudWatch.",
      "files": [],
      "expected_behavior": [
        "Defines a current-runtime perspective before rendering",
        "Creates an explicit resource inventory, containment map, and relationship allowlist",
        "Shows one schedule and one task rather than fabricating per-item schedules",
        "Labels each resource by descriptive name and AWS service type",
        "Keeps S3 and DynamoDB outside the VPC boundary while the Fargate task is inside it",
        "Inspects the generated image against every allowed and forbidden edge",
        "Saves and verifies an exact 3840x2160 PNG"
      ]
    },
    {
      "skills": ["creating-aws-architecture-diagrams"],
      "query": "Update our AWS architecture picture to show the current single-task design and a proposed SQS plus Fargate worker scale-out design. Put everything in one diagram.",
      "files": [],
      "expected_behavior": [
        "Rejects mixing current and future topology into an ambiguous master diagram",
        "Creates separate current-runtime and proposed-scale-out perspectives",
        "Keeps one schedule in both perspectives unless the stated architecture says otherwise",
        "Labels queue, worker, retry, and DLQ relationships explicitly in the scale-out view",
        "Does not imply that SQS directly starts ECS tasks unless an actual integration is specified",
        "Uses the same visual system across both artifacts"
      ]
    },
    {
      "skills": ["creating-aws-architecture-diagrams"],
      "query": "Generate an implementation-facing AWS architecture diagram from this repository's Terraform. Make it polished and use AWS icons.",
      "files": [],
      "expected_behavior": [
        "Reads Terraform and relevant documentation before deciding the topology",
        "Uses actual logical resource roles and does not expose secrets or copy sensitive account identifiers",
        "Separates runtime dependencies from deployment-time resources when both exist",
        "Uses current official AWS documentation to verify unstable service behavior",
        "Rejects any generated image whose arrows or boundaries disagree with Terraform",
        "Keeps a reviewable contract or Mermaid source beside the PNG"
      ]
    },
    {
      "skills": [],
      "query": "Draw a UML class diagram for these local Python data models.",
      "files": [],
      "expected_behavior": [
        "Does not invoke the AWS architecture diagram skill for a non-AWS UML class diagram"
      ]
    }
  ]
}
